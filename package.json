{
  "name": "ai-dev-team",
  "version": "0.1.0",
  "private": true,
  "description": "Model-agnostic AI dev team pipeline. A model-agnostic spine plus per-host adapters; one shared source of truth for templates, schemas, role briefs, and orchestrator logic.",
  "bin": {
    "devteam": "bin/devteam"
  },
  "scripts": {
    "test": "node --test tests/*.test.js",
    "consistency": "node scripts/consistency.js",
    "visualize": "node scripts/visualize.js",
    "pr-pack": "node scripts/pr-pack.js",
    "pr-publish": "node scripts/pr-publish.js",
    "dashboard": "node scripts/dashboard.js",
    "release:check": "node scripts/release.js check",
    "release:notes": "node scripts/release.js notes"
  },
  "dependencies": {
    "@opentelemetry/api": "^1.9.0",
    "@opentelemetry/exporter-trace-otlp-http": "^0.55.0",
    "@opentelemetry/resources": "^1.28.0",
    "@opentelemetry/sdk-node": "^0.55.0",
    "@opentelemetry/sdk-trace-base": "^1.28.0",
    "@opentelemetry/sdk-trace-node": "^1.30.1",
    "@opentelemetry/semantic-conventions": "^1.28.0",
    "js-yaml": "^4.1.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
